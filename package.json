{
  "name": "unreal-harness",
  "version": "0.1.0",
  "private": true,
  "description": "RSI-Harness: recursive self-improvement for coding agents, as an out-of-tree @deepseek-ai/dsh plugin family",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neeboo/unreal-harness.git"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p packages/judgment/tsconfig.json --noEmit && tsc -p packages/policy/tsconfig.json --noEmit",
    "judgment": "vitest run packages/judgment"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
