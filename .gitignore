# --- Rust ---
target/

# --- Node ---
node_modules/

# --- build output ---
lib/
dist/
*.tsbuildinfo

# --- test / coverage ---
coverage/
.vitest/

# --- benchmark runs ---
# The report page IS committed: it is a deliverable, and its input
# (measurements.json) is alongside it so a reader can check every figure. The
# markdown rendering is byte-for-byte reproducible from the same data and is the
# one artifact not committed.
bench-out/**/report.md

# --- local harness home (never commit a real one) ---
.dsh/
.dsh-home/

# --- editor / OS ---
.DS_Store
.idea/
*.swp
