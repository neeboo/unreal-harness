# --- Rust ---
target/

# --- Node ---
node_modules/

# --- build output ---
lib/
dist/
*.tsbuildinfo

# --- test / coverage ---
coverage/
.vitest/

# --- benchmark runs ---
# measurements.json IS committed (it is the evidence); the generated HTML is
# reproducible from it, so it is not.
bench-out/**/report.html
bench-out/**/report.md

# --- local harness home (never commit a real one) ---
.dsh/
.dsh-home/

# --- editor / OS ---
.DS_Store
.idea/
*.swp
