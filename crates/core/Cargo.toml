[package]
name = "unreal-harness-core"
description = "Pure, I/O-free decision types: a translator that cannot do I/O, durable versioned operations, and a context build that accounts for every reduction"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
